extends layout

block content
    div(class='container')
        div(class='page-header')
            h1 그룹조회
            h5 '그룹관리'에서는 한진EMR에 등록된 그룹 및 그룹원을 확인하실 수 있습니다.
        table(class="table table-striped table-bordered")
            thead
                tr
                    th(style='width: 30%;') 그룹명
                    th(style='width: 70%;') 구성원
            tbody
                tr
                if data != null
                    - for (var i = 0; i< data.length; i++)
                        tr
                            td= data[i].USER_GROUP
                            td= data[i].USER_NAME
